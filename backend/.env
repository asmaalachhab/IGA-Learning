# Variables Vite : à placer dans un fichier .env à la racine du dépôt frontend (voir .env.example).
# Port Tomcat par défaut documenté dans README_DEPLOYMENT.md.

VITE_API_URL=http://localhost:18080/iga-learning-api/api